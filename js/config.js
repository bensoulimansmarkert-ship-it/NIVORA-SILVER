/* NIVORA SILVER connection settings.
 * DEMO mode lets the static site run on GitHub Pages for preview/testing.
 * For production, set MODE to "api" and API_BASE_URL to your HTTPS backend URL.
 */
window.NIVORA_CONFIG = {
  MODE: "demo",
  API_BASE_URL: "https://YOUR-BACKEND-DOMAIN.example.com"
};
